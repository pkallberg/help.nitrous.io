<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[previewing | ]]></title>
  <link href="/categories/previewing/atom.xml" rel="self"/>
  <link href="/"/>
  <updated>2013-07-10T23:03:51+00:00</updated>
  <id>/</id>
  <author>
    <name><![CDATA[]]></name>
    
  </author>
  <generator uri="https://github.com/recurser/jekyll-plugins">Recurser</generator>

  
  <entry>
    <title type="html"><![CDATA[Previewing your application]]></title>
    <link href="/preview"/>
    <updated>2013-06-28T00:00:00+00:00</updated>
    <id>/preview</id>
    <content type="html"><![CDATA[<p>Nitrous.IO provides a range of ports <span>1024 - 9999</span> for you to run services on your development box.</p>

<p>Previewing your application simply involves taking an automatically generated preview url, pasting it into your browser, and appending the appropriate port where your service is running.</p>

<p>You can find the preview url from the following two locations:</p>

<ul>
<li><a href='#preview-webide'>Getting the Preview URL from the Web IDE</a></li>

<li><a href='#preview-boxindex'>Getting the Preview URL from the Boxes Dashboard</a></li>
</ul>

<h3 id='from_the_web_ide'>From the Web IDE</h3>

<p>The &#8220;Preview&#8221; menu in the <a href='/categories/web-ide'>Action Web IDE</a> displays links to some common default ports, which you can click to open a new window in your browser.</p>

<p><img alt='Preview Menu' src='https://raw.github.com/action-io/action-assets/master/support/screenshots/preview-menu.png' /></p>

<p>You can easily edit the urls from any of those links to preview other services you&#8217;re running by substituting your desired port.</p>

<h3 id='from_the_boxes_dashboard'>From the Boxes Dashboard</h3>

<p>You&#8217;ll also find your preview url in your boxes dashboard in the box-details.</p>

<p><img alt='Box Dashboard Preview URL' src='https://raw.github.com/action-io/action-assets/master/support/screenshots/box-preview-url.png' /></p>

<p>If you&#8217;re really smart, you&#8217;ll notice we use some sensible naming conventions for your box preview url. The box regions are probably tough to memorize, but regular users will remember it.</p>

<pre><code>https://*[box-name]*.*[box-region]*.nitrous.io:*[port]*</code></pre>

<h3 id='ruby_on_rails_example'>Ruby On Rails Example</h3>

<p>If you&#8217;re developing a Ruby on Rails project and using the default WEBrick web server, simply move into the Rails project directory in your console and run:</p>

<pre><code>rails server</code></pre>

<p>This will fire up an instance of the WEBrick web server by default (Rails can also use several other web servers). To see your application in action, click the &#8220;Preview&#8221; menu and select &#8220;Port 3000&#8221;. You should see your Rails development project in the new window that is opened.</p>

<h3 id='other_frameworks'>Other Frameworks</h3>

<p>For help on previewing applications written in other languages and frameworks, check the following <a href='/faq-localhost'>support article</a>.</p>]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Problems previewing my application]]></title>
    <link href="/faq-localhost"/>
    <updated>2013-06-28T00:00:00+00:00</updated>
    <id>/faq-localhost</id>
    <content type="html"><![CDATA[<p>Due to the way we handle network configuration on boxes, you will not be able to bind http servers to localhost (127.0.0.1).</p>

<p>Your application will need to run on 0.0.0.0 or a specific IP address. Listening on port 3000 will allow you to use our preview menu for a shortcut.</p>

<h3 id='nodejs_example'>Node.js example</h3>

<p>Setting up a hello world web server on port 3000 using node.js.</p>
<pre class='prettyprint lang-javascript'>
  var http = require('http');
  http.createServer(function (req, res) {
    res.writeHead(200, {'Content-Type': 'text/plain'});
    res.end('Hello World\n');
  }).listen(3000, '0.0.0.0');
  console.log('Server running at http://0.0.0.0:3000/');
</pre>
<p>Then make sure to run your node server in the console (substitute your file name):</p>

<pre><code>node helloworld.js</code></pre>

<h3 id='python_example'>Python example</h3>

<p>When running a simple development server with <a href='https://docs.djangoproject.com/en/dev/intro/'>Django</a>, we recommend:</p>

<pre><code>python manage.py runserver 0.0.0.0:3000</code></pre>

<h3 id='python_on_google_app_engine'>Python on Google App Engine</h3>

<p>When running the development app server on <a href='https://developers.google.com/appengine/docs/python/gettingstarted/'>Google App Engine</a>, we recommend:</p>

<pre><code>devappserver.py --address=0.0.0.0 --port=3000</code></pre>

<h3 id='previewing_the_application'>Previewing the application</h3>

<p>We supply some sensible default ports to enable you to preview your work in the browser. You can get your preview link in 1 of 2 ways:</p>

<p>Launch the Web IDE for your box, click the &#8220;Preview&#8221; menu and select the port where your web server is running.</p>

<p><img alt='Preview Menu' src='https://raw.github.com/action-io/action-assets/master/support/screenshots/preview-menu.png' /></p>

<p>In your list of boxes, click the box name to toggle it&#8217;s details, copy the Preview URL and paste it in your browser. Change the port to 3000 or the applicable port where your server is running.</p>

<p><img alt='Box Preview URL' src='https://raw.github.com/action-io/action-assets/master/support/screenshots/box-preview-url.png' /></p>]]></content>
  </entry>
  
</feed>